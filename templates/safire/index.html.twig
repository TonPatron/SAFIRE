{% extends 'base.html.twig' %}


{% block body %}


	{# style DEBUT #}
	<style>
		.parallax {
			/* The image used */

			background-image: url("{{asset('asset/image/serie.jpg')}}");

			/* Full height */
			height: 350px;

			/* Create the parallax scrolling effect */
			background-attachment: fixed;
			background-position: center;
			background-repeat: no-repeat;
			background-size: cover;
		}

		.parallax1 {
			/* The image used */

			background-image: url("{{asset('asset/image/anime.jpg')}}");

			/* Full height */
			height: 350px;

			/* Create the parallax scrolling effect */
			background-attachment: fixed;
			background-position: center;
			background-repeat: no-repeat;
			background-size: cover;
		}

		.parallax2 {
			/* The image used */

			background-image: url("{{asset('asset/image/film.jpg')}}");

			/* Full height */
			height: 350px;

			/* Create the parallax scrolling effect */
			background-attachment: fixed;
			background-position: center;
			background-repeat: no-repeat;
			background-size: cover;
		}
	</style>
	{# style FIN #}


	{# Bouton DEBUT #}

	<div class="main" id="section1">

		<a href="#section2">
			<i class="fas fa-chevron-double-down" style="color: red; display: flex; justify-content: center; width: 100%; height: 50px;align-items: center;"></i>
		</a>

	</div>

	{# Bouton FIN #}

	<main
		class="clr">
		{# parallax serie DEBUT #}
		<div class="parallax "></div>

		<div>


			{#Debut de nos Séries / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / #}

			<div>
				{% set n = 0 %}

				<a href="{{path('afficheAllSeries')}}">
					<h4 class="h42">
						Les Series</h4>
				</a>
			</div>

			<div
				id="carouselSerie" class="carousel slide carousel-multi-item" data-ride="carousel">


				<!--Controls-->
				<div class="controls-top">
					<a class="btn-floating" href="#carouselSerie" data-slide="prev">
						<i class="fa fa-chevron-left"></i>
					</a>
					<a class="btn-floating" href="#carouselSerie" data-slide="next">
						<i class="fa fa-chevron-right"></i>
					</a>
				</div>
				<!--/.Controls-->

				<!--Slides-->
				<div
					class="carousel-inner" role="listbox">


					{# premier slide #}

					<div class="carousel-item active">
						<div class="row">
							<div class="col-md-4">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
						</div>

					</div>
					{% set n = n +1 %}

					{# second slide #}

					<div class="carousel-item">
						<div class="row">
							<div class="col-md-4">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
						</div>

					</div>

					{% set n = n +1 %}

					{# troisieme slide #}

					<div class="carousel-item">
						<div class="row">
							<div class="col-md-4">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
												<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
											</a>
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLaSerie', {'id':series[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageSeries/" ~ series[n].imageSerie)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{series[n].titre}}</h5>
										<p class="card-text">Sort le:{{series[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
						</div>

					</div>
					{% set n = n +1 %}

				</div>
			</div>

			{# fin serie ///////////////////////////////////////////////////////////////// #}


		</div>
		{# parallax FIN #}


		{# parallax1 anime DEBUT #}

		<div class="parallax1"></div>

		<div>


			{#LES PROCHAINS ANIMES EN VU #}

			<div>
				{% set n = 0 %}

				<a href="{{path('afficheAllAnimes')}}">
					<h4 class="h42">Les Animes</h4>
				</a>
			</div>

			<div
				id="carouselAnime" class="carousel slide carousel-multi-item" data-ride="carousel">

				<!--Controls-->
				<div class="controls-top">
					<a class="btn-floating" href="#carouselAnime" data-slide="prev">
						<i class="fa fa-chevron-left"></i>
					</a>
					<a class="btn-floating" href="#carouselAnime" data-slide="next">
						<i class="fa fa-chevron-right"></i>
					</a>
				</div>
				<!--/.Controls-->

				<!--Slides-->
				<div
					class="carousel-inner" role="listbox">

					{# premier slide #}

					<div class="carousel-item active">
						<div class="row">
							<div class="col-md-4">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
						</div>

					</div>
					{% set n = n +1 %}

					{# second slide #}

					<div class="carousel-item">
						<div class="row">
							<div class="col-md-4">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
						</div>

					</div>
					{% set n = n +1 %}

					{# troisieme slide #}

					<div class="carousel-item">
						<div class="row">
							<div class="col-md-4">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLanime', {'id':animes[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageAnimes/" ~ animes[n].imageAnime)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{animes[n].titre}}</h5>
										<p class="card-text">Sort le:{{animes[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
						</div>

					</div>
					{% set n = n +1 %}
				</div>
			</div>

			{#Fin de nos Animes / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / #}

		</div>
		{# parallax1 FIN #}


		{# parallax2 film DEBUT #}
		<div class="parallax2"></div>

		<div>


			{#Debut de nos films / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / #}

			<div>
				{% set n = 0 %}
				<a href="{{path('afficheAllFilms')}}">
					<h4 class="h42">Les Films</h4>
				</a>
			</div>
			<div
				id="carouselFilm" class="carousel slide carousel-multi-item" data-ride="carousel">

				<!--Controls-->
				<div class="controls-top">
					<a class="btn-floating" href="#carouselFilm" data-slide="prev">
						<i class="fa fa-chevron-left"></i>
					</a>
					<a class="btn-floating" href="#carouselFilm" data-slide="next">
						<i class="fa fa-chevron-right"></i>
					</a>
				</div>
				<!--/.Controls-->

				<!--Slides-->
				<div class="carousel-inner" role="listbox">

					<div class="carousel-item active">
						<div class="row">
							<div class="col-md-4">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">{{films[n].titre}}</h5>
										<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{films[n].titre}}</h5>
										<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{films[n].titre}}</h5>
										<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
						</div>

					</div>
					{% set n = n +1 %}

					{# second slide #}

					<div class="carousel-item">
						<div class="row">
							<div class="col-md-4">
								<div class="card mb-2">
									<div class="toopit">
										<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
											<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{films[n].titre}}</h5>
										<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="">
											<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
												<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
											</a>
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{films[n].titre}}</h5>
										<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
							{% set n = n +1 %}
							<div class="col-md-4 clearfix d-none d-md-block">
								<div class="card mb-2">
									<div class="toopit">
										<a href="">
											<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
												<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
											</a>
										</a>
									</div>
									<div class="card-body">
										<h5 class="card-title">Titre:{{films[n].titre}}</h5>
										<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
									</div>
								</div>
							</div>
						</div>

					</div>
					{% set n = n +1 %}


				</div>


				{# troisieme slide #}

				<div class="carousel-item">
					<div class="row">
						<div class="col-md-4">
							<div class="card mb-2">
								<div class="toopit">
									<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
										<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
									</a>
								</div>
								<div class="card-body">
									<h5 class="card-title">Titre:{{films[n].titre}}</h5>
									<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
								</div>
							</div>
						</div>
						{% set n = n +1 %}
						<div class="col-md-4 clearfix d-none d-md-block">
							<div class="card mb-2">
								<div class="toopit">
									<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
										<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
									</a>
								</div>
								<div class="card-body">
									<h5 class="card-title">Titre:{{films[n].titre}}</h5>
									<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
								</div>
							</div>
						</div>
						{% set n = n +1 %}
						<div class="col-md-4 clearfix d-none d-md-block">
							<div class="card mb-2">
								<div class="toopit">
									<a href="{{path('afficheLeFilm', {'id':films[n].id})}}">
										<img width="100%" src="{{asset("uploads/imageFilms/" ~ films[n].imageFilm)}}">
									</a>
								</div>
								<div class="card-body">
									<h5 class="card-title">Titre:{{films[n].titre}}</h5>
									<p class="card-text">Sort le:{{films[n].dateDeSortieAt| date("m/d/Y")}}</p>
								</div>
							</div>
						</div>
					</div>

				</div>
				{% set n = n +1 %}
			</div>

		</div>

		{#Fin de nos Films / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / / #}


		{# parallax2 FIN #}


		{# Bouton DEBUT #}

			<div class="main" id="section2"> <a href="#section1">
				<i class="fas fa-chevron-double-up" style="color: red; display: flex; justify-content: center; width: 100%; height: 50px; align-items: center"></i>
			</a>

		</div>
		{# Bouton FIN #}
	</main>

	<div class="container-fluid">
		<div class="row">


			<div class="about col-12">

				<p>À PROPOS DE NOUS</p>
				<p>
					Nous avons créer ce site dans le but de réunir toutes les prochaines sorties de FILMS,SERIES et ANIMÉS. 
					 Vous trouverez ici donc toutes les informations necésaire sur les prochaines sorties : date, genre ou encore sur quelle plateforme les retrouver.
				</p>


			</div>


		</div>
	</div>

{% endblock %}
